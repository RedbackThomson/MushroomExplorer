import { PlaceholderRoute } from '@/components/PlaceholderRoute';

export default function Items() {
  return (
    <PlaceholderRoute
      title="Items"
      description="Use, Setup, Etc, and Cash items — populated from your local game files."
    />
  );
}
