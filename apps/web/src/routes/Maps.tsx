import { PlaceholderRoute } from '@/components/PlaceholderRoute';

export default function Maps() {
  return (
    <PlaceholderRoute
      title="Maps"
      description="Map pages listing NPCs, mobs, portals, and connected regions."
    />
  );
}
