import { PlaceholderRoute } from '@/components/PlaceholderRoute';

export default function Mobs() {
  return (
    <PlaceholderRoute
      title="Mobs"
      description="Monsters with level, HP, EXP, elemental properties, and spawn maps."
    />
  );
}
