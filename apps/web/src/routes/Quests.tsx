import { PlaceholderRoute } from '@/components/PlaceholderRoute';

export default function Quests() {
  return (
    <PlaceholderRoute
      title="Quests"
      description="Quest pages with start/end NPCs, prerequisites, requirements, and rewards."
    />
  );
}
