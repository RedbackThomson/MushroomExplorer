import { PlaceholderRoute } from '@/components/PlaceholderRoute';

export default function Npcs() {
  return (
    <PlaceholderRoute
      title="NPCs"
      description="NPCs with their map locations and linked quests or shops."
    />
  );
}
